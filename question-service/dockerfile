# Use Node.js LTS
FROM node:22

# Set working directory
WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Expose port (match your server)
EXPOSE 3000

# Start the app
CMD ["npm", "start"]
